diff --git a/node_modules/jsdom/lib/jsdom/level1/core.js b/lib/jsdom/level1/core.js
index ee852ab..96d7570 100644
--- a/node_modules/jsdom/lib/jsdom/level1/core.js
+++ b/node_modules/jsdom/lib/jsdom/level1/core.js
@@ -996,10 +996,10 @@ core.Element.prototype = {
             if (self.run != undefined) {
                 // We're the window. This can happen because inline handlers
                 // on the body are proxied to the window.
-                self.run(value);
+                // self.run(value);
             } else {
                 // We're an element.
-                self._ownerDocument.parentWindow.run(value);
+                // self._ownerDocument.parentWindow.run(value);
             }
         };
     }
